rules_version = '2';

// Règles de sécurité Cloud Storage — Campus Events
// Lecture et écriture réservées aux utilisateurs connectés.

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
